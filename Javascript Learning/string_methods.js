// trim, toUpperCase(), toLowerCase(), slice

let firstName = "   vidhi   ";

// to remove the spaces in a string we can use trim method

let newString = firstName.trim();

console.log(newString.length);

// slice method take first index and go till last index -1

let name = "pooja";
let newString1 = name.slice(0,4);
console.log(newString1); // pooj