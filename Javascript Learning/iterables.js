// jispe hum for of loop laga sakte hai
// string, array are iterable

const firstName = "vidhi";

for(let char of firstName) {
    console.log(char);
}

// objects are not iterable
// array like objects are those which have length property and we can access it using index
// example: string